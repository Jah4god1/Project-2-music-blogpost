{{!--
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Classify.mood</title>
  <link rel="stylesheet" href="../../public/css/miccitest.css">
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>

<body>
  <!-- Navigation Bar -->
  <nav class="navbar navbar-expand-lg navbar-purple ">
    <a class="navbar-brand" href="#">Music Blog</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <style>
      .navbar-purple {
        background-color: rgba(248, 226, 255, 0.2);
      }
    </style>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        {{#each links}}
        <li class="nav-item {{#if this.active}}active{{/if}}">
          <a class="nav-link" href="{{this.href}}">{{this.text}}</a>
        </li>
        {{/each}}
      </ul>
    </div>
  </nav>

  <!-- Main Content -->
  <div class="image-container">
    <img src="{{mainImage}}" class="img-fluid" alt="Image 1">
  </div>
  <div class="container">

    <div class="row">
      <div class="col-md-3">
        {{#each headings}}
        <div class="row">
          <div class="col">
            <h1 class="text-bold">{{this}}</h1>
          </div>
        </div>
        {{/each}}
        <button class="btn btn-primary"><a href="{{accountCreationLink}}" style="color:white">Create
            Account</a></button>

      </div>
      <div class="col-md-6">
        <img src="{{secondaryImage}}" class="img-fluid" alt="Image 2">
      </div>
    </div>
    <hr class="invisible-hr"> <!-- Line separating the sections -->

    <!-- music posts today section  -->
    <div class="container">
      <div class="row">
        <div class="col-md-4">
          <div class="row">
            <div class="col">
              <h1 class="text-bold">Music</h1>
              <h1 class="text-bold text-white">Posts</h1>
            </div>
          </div>
          <div class="row">
            <div class="col">
              <h1 class="text-bold">Today</h1>
            </div>
          </div>
          <button class="btn btn-outline-light"><a href="{{postsLink}}" style="color:white">See All Posts</a></button>

        </div>
        <div class="col-md-6">
          <!-- row of pics -->
          <div class="container">
            <div class="row">
              {{#each images}}
              <div class="col">
                <img src="{{this}}" class="img-fluid" alt="Image">
              </div>
              {{/each}}
            </div>
          </div>
        </div>
      </div>

      <script src="../../public/signup.js"></script>
</body>

</html> --}}

<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fund My Project!</title>
  <link rel="stylesheet" href="../../public/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="flex-column justify-space-around">
    <header class="display-flex justify-space-between align-center p-2">
      <h1>

        <a href="/">music blog</a>
      </h1>
      <nav>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/userhome">User Home</a> |
        <button class="no-button" id="logout">Logout</button>
        {{else}}
        <a href="../signup.handlebars">Signup</a> |
        <a href="../login.handlebars">Login</a>
        {{/if}}

      </nav>
    </header>
    {{!-- <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main> --}}
    <footer>

    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="../../public/logout.js"></script>
  {{/if}}
</body>

</html>